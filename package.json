{
  "scripts": {
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm ci --progress=false && npm run build && cd .. && node frontend/scripts/remove-cloudflare-redirects.js && npx wrangler deploy",
    "deploy:dry-run": "cd frontend && npm ci --progress=false && npm run build && cd .. && node frontend/scripts/remove-cloudflare-redirects.js && npx wrangler deploy --dry-run"
  },
  "devDependencies": {
    "supabase": "^2.98.2"
  }
}
